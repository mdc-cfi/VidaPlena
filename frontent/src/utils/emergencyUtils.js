export function sendEmergencyAlert(cliente) {
  // Simulación de envío de alerta de emergencia
  console.log(`Enviando alerta de emergencia para el cliente: ${cliente.nombre}`);
  // Aquí podrías integrar servicios como Twilio, Firebase Cloud Messaging, etc.
}